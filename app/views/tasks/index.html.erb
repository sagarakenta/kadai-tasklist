<h1>一覧</h1>
<ul>
  <% @task.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %></li>
    
  <% @tasks.each do |tasks| %>
    <li><%= link_to task.id, task %> : <%= tasks.status %></li>
  
  <% end %>
</ul>

<%= link_to '新規の投稿', new_task_path %>